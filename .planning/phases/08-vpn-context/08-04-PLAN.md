---
phase: 08-vpn-context
plan: 04
type: execute
wave: 3
depends_on:
  - 08-02
  - 08-03
files_modified:
  - CellGuard/Views/EventDetailView.swift
  - CellGuard/Views/DashboardView.swift
autonomous: true
requirements:
  - VPN-01
  - VPN-02
  - VPN-03
tags:
  - ios26
  - swiftui
  - vpn
  - ui
  - privacy
  - dashboard

must_haves:
  truths:
    - "EventDetailView shows a new Section('VPN') between Section('Wi-Fi') and Section('Probe') when event.vpnState is in {.connecting, .connected, .reasserting, .disconnecting}"
    - "EventDetailView's new VPN section is hidden when event.vpnState is nil, .disconnected, or .invalid (UI-SPEC §Nil/Empty State Contract)"
    - "EventDetailView contains a VPNState.displayName extension matching UI-SPEC table — Reconnecting (NOT Reasserting) for the .reasserting case"
    - "DashboardView's connectivity-state-card interface label uses monitor.effectiveInterfaceLabel (instead of monitor.currentInterfaceType.displayName)"
    - "DashboardView's privacy toggle copy reads 'Omit location, Wi-Fi, and VPN data' (Oxford comma, mentioning all three categories)"
    - "EventDetailView's Network section 'Interface' row stays as raw event.interfaceType.displayName (UI-SPEC: detail view shows ground truth, dashboard-only override)"
  artifacts:
    - path: "CellGuard/Views/EventDetailView.swift"
      provides: "VPN section + VPNState.displayName extension"
      contains: "Section(\"VPN\")"
    - path: "CellGuard/Views/DashboardView.swift"
      provides: "interface label flip via effectiveInterfaceLabel + privacy toggle copy update"
      contains: "monitor.effectiveInterfaceLabel"
  key_links:
    - from: "EventDetailView.Section('VPN')"
      to: "event.vpnState (VPNState?)"
      via: "if let state = event.vpnState, state != .disconnected, state != .invalid"
      pattern: "Section\\(\"VPN\"\\)"
    - from: "DashboardView.connectivityStateCard.Text"
      to: "ConnectivityMonitor.effectiveInterfaceLabel"
      via: "Text(monitor.effectiveInterfaceLabel)"
      pattern: "monitor\\.effectiveInterfaceLabel"
    - from: "DashboardView.Toggle"
      to: "AppStorage omitLocationData"
      via: "Toggle('Omit location, Wi-Fi, and VPN data', isOn: $omitLocation) — label change only, gating logic unchanged"
      pattern: "Omit location, Wi-Fi, and VPN data"
---

<objective>
Wire the VPN data produced by Plans 02 and 03 to the user-visible surfaces. After this plan, the user can open any event and see the VPN state, see "VPN" on the dashboard interface card when a tunnel is up, and read a privacy toggle that explicitly mentions VPN.

Purpose: Deliver VPN-01 (event log shows VPN state via the new section), VPN-02 (dashboard label flip via `effectiveInterfaceLabel`), and the user-facing portion of VPN-03 (privacy toggle copy that names VPN). The actual export gating already lives in Plan 02's `ConnectivityEvent.encode`.

Output: `CellGuard/Views/EventDetailView.swift` with a conditional `Section("VPN")` and a `VPNState.displayName` extension; `CellGuard/Views/DashboardView.swift` with the interface label and toggle copy substitutions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-vpn-context/08-CONTEXT.md
@.planning/phases/08-vpn-context/08-PATTERNS.md
@.planning/phases/08-vpn-context/08-UI-SPEC.md
@.planning/phases/08-vpn-context/08-02-SUMMARY.md
@.planning/phases/08-vpn-context/08-03-SUMMARY.md

@CellGuard/Views/EventDetailView.swift
@CellGuard/Views/DashboardView.swift

<interfaces>
<!-- The exact code shapes Plan 04 must produce. Extracted verbatim from
     08-PATTERNS.md and 08-UI-SPEC.md so the executor does not need to
     re-derive copy or placement. -->

**EventDetailView.swift — Edit 1: Add the conditional VPN Section.**

Place IMMEDIATELY AFTER the existing `Section("Wi-Fi")` block at lines 30-34, BEFORE `Section("Probe")` at line 36. The new section uses the section ordering Event → Network → Cellular → Wi-Fi → **VPN** → Probe → Location → Duration (08-PATTERNS.md Pattern 2 in EventDetailView.swift):

```swift
// (Existing — line 30-34, do not change)
if event.wifiSSID != nil {
    Section("Wi-Fi") {
        LabeledContent("SSID", value: event.wifiSSID?.isEmpty == true ? "\u{2014}" : event.wifiSSID!)
    }
}

// NEW — VPN section, visible only for non-trivial states (D-05; UI-SPEC §Nil/Empty State Contract)
if let state = event.vpnState,
   state != .disconnected,
   state != .invalid {
    Section("VPN") {
        LabeledContent("State", value: state.displayName)
    }
}

// (Existing — line 36 onwards, do not change)
```

Visibility rule (UI-SPEC §Nil/Empty State Contract):
- `nil` → section hidden
- `.disconnected` → section hidden
- `.invalid` → section hidden
- `.connecting | .connected | .reasserting | .disconnecting` → section visible

Copy rules (UI-SPEC §Copywriting Contract):
- Section header: `"VPN"` (all caps, no punctuation, matches `"Wi-Fi"` / `"Cellular"` / `"Probe"`)
- Field label: `"State"` (single capitalized noun, NOT "Status" — `Status` is already used by Network section's `Path Status` row)
- Value: `state.displayName` from the extension (Edit 2 below)
- Never show `"—"`, `"N/A"`, or `"Unknown"` — hide instead.

**EventDetailView.swift — Edit 2: Add the `VPNState.displayName` extension.**

Place IMMEDIATELY AFTER the existing `extension InterfaceType { var displayName: ... }` at lines 110-122. UI-SPEC table at lines 112-119 locks these strings — `Reconnecting` (NOT `Reasserting`) for `.reasserting` is the single deliberate divergence from a literal title-case of the enum name:

```swift
extension VPNState {
    /// Human-readable name for UI display. The 6-state model projects to the
    /// spec's 3-state vocabulary via `Reconnecting` (= reasserting). Per UI-SPEC §Copywriting
    /// Contract, `Reconnecting` is the human-readable mapping for `.reasserting`; never expose
    /// the internal Apple-API word "reasserting" to users.
    var displayName: String {
        switch self {
        case .invalid: "Invalid"
        case .disconnected: "Disconnected"
        case .connecting: "Connecting"
        case .connected: "Connected"
        case .reasserting: "Reconnecting"
        case .disconnecting: "Disconnecting"
        }
    }
}
```

**DashboardView.swift — Edit 3: Replace the interface label binding.**

Existing code at line ~180:
```swift
Text(monitor.currentInterfaceType.displayName)
    .font(.headline)
```

Replace `monitor.currentInterfaceType.displayName` with `monitor.effectiveInterfaceLabel`. NO styling change — `.headline`, `.primary` foreground stay identical (UI-SPEC §DashboardView lines 210, 214 — "no bold override, no italic, no color"):
```swift
Text(monitor.effectiveInterfaceLabel)
    .font(.headline)
```

> The `effectiveInterfaceLabel` property is added in Plan 03. It returns `"VPN"` when `currentVPNState ∈ {.connected, .reasserting}` and otherwise returns `currentInterfaceType.displayName`. Because `ConnectivityMonitor` is `@Observable`, SwiftUI re-renders this `Text` whenever either `currentVPNState` or `currentInterfaceType` changes.

**DashboardView.swift — Edit 4: Update privacy toggle copy.**

Existing code at line ~97:
```swift
Toggle("Omit location and Wi-Fi data", isOn: $omitLocation)
```

Replace the literal string ONLY. All modifiers, the `$omitLocation` binding, and the AppStorage key (`@AppStorage("omitLocationData")` at line 15) are unchanged:
```swift
Toggle("Omit location, Wi-Fi, and VPN data", isOn: $omitLocation)
```

Copy rationale (UI-SPEC §Privacy Toggle Label Update):
- Oxford comma form: `"location, Wi-Fi, and VPN"` reads better than `"location and Wi-Fi and VPN"` and scales to a fourth category later.
- Mentions all three categories explicitly — Phase 7 set the precedent of naming gated categories rather than abstracting to "personal context."
- The user's UI-feedback memory (`project_ui_feedback.md`) flags that the privacy toggle scope must be visible in its label.

**Surfaces explicitly NOT changed in this plan (UI-SPEC lock):**

- `EventDetailView`'s Network-section `Interface` row — KEEPS `event.interfaceType.displayName` (raw ground truth). The dashboard-only `effectiveInterfaceLabel` override does NOT propagate to detail view (UI-SPEC §"Where the override applies vs. does not" — table entry: EventDetailView Network section "Interface" row → No → "Detail view shows ground truth").
- `EventListView` rows — no change. Lists do not show interface today and Phase 8 does not add it (UI-SPEC §EventListView — No Change).
- `EventLogExport.swift` — no change. The encoder gate in `ConnectivityEvent.encode` (Plan 02) handles export-side privacy.
- The `InterfaceType` enum — no `.vpn` case (D-04, anti-pattern guard).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add VPN section + VPNState.displayName to EventDetailView</name>
  <files>CellGuard/Views/EventDetailView.swift</files>
  <read_first>
    - CellGuard/Views/EventDetailView.swift (FULL FILE — line 30-34 Wi-Fi section is the line-for-line template; lines 99-122 displayName extensions co-located with view; line 36 onwards probe section is the placement boundary)
    - .planning/phases/08-vpn-context/08-PATTERNS.md (lines 388-465: EventDetailView.swift Patterns 1, 2, 3)
    - .planning/phases/08-vpn-context/08-UI-SPEC.md (lines 105-144 State Display Projection table; lines 146-186 EventDetailView VPN section spec; lines 324-342 Nil/Empty State Contract; lines 346-382 Copywriting Contract — `Reconnecting` not `Reasserting`)
    - .planning/phases/08-vpn-context/08-CONTEXT.md (D-05: section visibility threshold `vpnState ∉ {disconnected, invalid}`)
    - CellGuard/Models/ConnectivityEvent.swift (verify Plan 02's `VPNState` enum and `event.vpnState: VPNState?` accessor exist)
  </read_first>
  <action>
    Make exactly two edits to `CellGuard/Views/EventDetailView.swift`:

    1. **Insert the VPN section** (interfaces Edit 1) immediately after the `Section("Wi-Fi")` block at lines 30-34. The new section visibility uses `if let state = event.vpnState, state != .disconnected, state != .invalid`. One row inside: `LabeledContent("State", value: state.displayName)`. Do NOT add an empty-string fallback or em-dash — when the section's not visible it's hidden entirely (UI-SPEC line 179, 339).

    2. **Insert the `VPNState.displayName` extension** (interfaces Edit 2) immediately after the existing `extension InterfaceType { var displayName: ... }` block at lines 110-122. The 6 cases map to the UI-SPEC table strings, with `Reconnecting` (NOT `Reasserting`) for `.reasserting` — this is the single deliberate divergence and is required.

    Do NOT touch:
    - The `Section("Network")`'s `Interface` row at line ~17 (it stays as `event.interfaceType.displayName`, ground truth — UI-SPEC §"Where the override applies vs. does not").
    - `EventListView` (no changes anywhere in Phase 8).
    - The existing Wi-Fi, Cellular, Probe, Location, Duration sections.

    Casing/copy invariants:
    - Section header is `"VPN"` (all caps).
    - Field label is `"State"` (NOT `"Status"`, NOT `"VPN State"`).
    - `displayName` for `.reasserting` is `"Reconnecting"` exactly.
  </action>
  <verify>
    <automated>cd /Users/mowehr/code/cellguard &amp;&amp; xcrun xcodebuild -scheme CellGuard -destination 'generic/platform=iOS' -configuration Debug build 2>&amp;1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'Section("VPN")' CellGuard/Views/EventDetailView.swift` returns one match.
    - `grep -nE 'state != \.disconnected.*state != \.invalid|state != \.invalid.*state != \.disconnected' CellGuard/Views/EventDetailView.swift` returns one match (the visibility guard).
    - `grep -nE 'LabeledContent\("State", value: state\.displayName\)' CellGuard/Views/EventDetailView.swift` returns one match.
    - `grep -n "extension VPNState" CellGuard/Views/EventDetailView.swift` returns one match.
    - `grep -nE 'case \.reasserting:.*"Reconnecting"' CellGuard/Views/EventDetailView.swift` returns one match (NOT "Reasserting").
    - `grep -nE 'case \.connected:.*"Connected"' CellGuard/Views/EventDetailView.swift` returns one match.
    - `grep -nE 'case \.connecting:.*"Connecting"' CellGuard/Views/EventDetailView.swift` returns one match.
    - `grep -nE 'case \.disconnecting:.*"Disconnecting"' CellGuard/Views/EventDetailView.swift` returns one match.
    - `grep -nE 'case \.disconnected:.*"Disconnected"' CellGuard/Views/EventDetailView.swift` returns one match.
    - `grep -nE 'case \.invalid:.*"Invalid"' CellGuard/Views/EventDetailView.swift` returns one match.
    - `! grep -i "Reasserting" CellGuard/Views/EventDetailView.swift` confirms the literal word "Reasserting" does NOT appear in any user-facing string (the enum case name `.reasserting` is fine, the displayed string must be `Reconnecting`).
    - `xcrun xcodebuild ... build` exits 0.
  </acceptance_criteria>
  <done>VPN section appears between Wi-Fi and Probe; VPNState.displayName provides Reconnecting (not Reasserting); build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update DashboardView interface label and privacy toggle copy</name>
  <files>CellGuard/Views/DashboardView.swift</files>
  <read_first>
    - CellGuard/Views/DashboardView.swift (FULL FILE — line 97 privacy toggle, line 180 interface label inside connectivityStateCard at lines 166-187, line 15 @AppStorage declaration)
    - .planning/phases/08-vpn-context/08-PATTERNS.md (lines 467-525: DashboardView.swift Patterns 1 and 2)
    - .planning/phases/08-vpn-context/08-UI-SPEC.md (lines 188-237 DashboardView Interface Label Substitution; lines 298-322 Privacy Toggle Label Update)
    - .planning/phases/08-vpn-context/08-03-SUMMARY.md (verify monitor.effectiveInterfaceLabel was added in Plan 03)
    - CellGuard/Services/ConnectivityMonitor.swift (verify Plan 03's `effectiveInterfaceLabel` is in scope)
  </read_first>
  <action>
    Make exactly two single-line substitutions in `CellGuard/Views/DashboardView.swift`:

    1. **Line ~180 — interface label binding** (interfaces Edit 3): change `Text(monitor.currentInterfaceType.displayName)` to `Text(monitor.effectiveInterfaceLabel)`. Do NOT change `.font(.headline)` or any other modifier. Do NOT add color, italic, animation, or a "Reconnecting" pill — UI-SPEC §"Animation/transition for reasserting" lines 212-220 explicitly forbid these on the dashboard.

    2. **Line ~97 — privacy toggle copy** (interfaces Edit 4): change the literal string `"Omit location and Wi-Fi data"` to `"Omit location, Wi-Fi, and VPN data"`. Do NOT change `isOn: $omitLocation`, the `@AppStorage("omitLocationData")` key at line 15, or any modifier — copy ONLY.

    Do NOT touch:
    - The `connectivityStateCard`'s "Current Status" `Text` (line ~172, the path status) — unchanged.
    - The `omitLocation` AppStorage key name (`omitLocationData`) — unchanged; renaming would break privacy persistence.
    - Any other view in DashboardView.
    - The chart, health indicators, ShareLink, or any other Phase 1-7 surface.
  </action>
  <verify>
    <automated>cd /Users/mowehr/code/cellguard &amp;&amp; xcrun xcodebuild -scheme CellGuard -destination 'generic/platform=iOS' -configuration Debug build 2>&amp;1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "monitor.effectiveInterfaceLabel" CellGuard/Views/DashboardView.swift` returns one match.
    - `! grep -n "monitor.currentInterfaceType.displayName" CellGuard/Views/DashboardView.swift` confirms the old binding is fully replaced (no stragglers).
    - `grep -nF 'Toggle("Omit location, Wi-Fi, and VPN data"' CellGuard/Views/DashboardView.swift` returns one match (Oxford comma form, exact string).
    - `! grep -nF 'Toggle("Omit location and Wi-Fi data"' CellGuard/Views/DashboardView.swift` confirms the old toggle copy is fully replaced.
    - `grep -n '@AppStorage("omitLocationData")' CellGuard/Views/DashboardView.swift` still returns one match (key name unchanged).
    - `grep -n "$omitLocation" CellGuard/Views/DashboardView.swift` still finds the existing toggle binding.
    - `xcrun xcodebuild ... build` exits 0.
  </acceptance_criteria>
  <done>Dashboard interface label flips to "VPN" via the live binding; privacy toggle copy explicitly mentions all three gated categories; build passes.</done>
</task>

</tasks>

<verification>
- Build succeeds on both edits.
- All grep-based acceptance criteria pass.
- The dashboard `Text` binding now reads from `monitor.effectiveInterfaceLabel` (live `@Observable` re-render path).
- The privacy toggle copy is the exact UI-SPEC-locked string.
- EventDetailView's Network section "Interface" row was NOT changed — `git diff CellGuard/Views/EventDetailView.swift` shows changes ONLY in the new VPN section block and the new `extension VPNState` block.
</verification>

<success_criteria>
- [ ] `Section("VPN")` exists in EventDetailView between Wi-Fi and Probe sections.
- [ ] Visibility guard: `if let state = event.vpnState, state != .disconnected, state != .invalid`.
- [ ] `extension VPNState { var displayName }` provides `Reconnecting` for `.reasserting`.
- [ ] DashboardView interface label uses `monitor.effectiveInterfaceLabel`.
- [ ] DashboardView privacy toggle copy is `"Omit location, Wi-Fi, and VPN data"`.
- [ ] No changes to EventDetailView's Network "Interface" row (still raw `interfaceType.displayName`).
- [ ] No changes to EventListView, EventLogExport, or InterfaceType enum.
- [ ] Build succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/08-vpn-context/08-04-SUMMARY.md` covering:
- The four edits (2 in EventDetailView, 2 in DashboardView) with file regions.
- The literal copy strings used (Section header, field label, `Reconnecting`, toggle string).
- Confirmation that the dashboard-only override is NOT applied to detail view, list, or export.
- A pointer to the human UAT checklist that should follow phase completion (per Phase 7 precedent: open an event after a VPN-active drop, observe the new section; flip the privacy toggle and re-export).
</output>
