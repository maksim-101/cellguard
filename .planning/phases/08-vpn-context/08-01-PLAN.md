---
phase: 08-vpn-context
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/08-vpn-context/08-VERIFICATION-WAVE-0.md
autonomous: false
requirements: []
tags:
  - ios26
  - vpn
  - device-verification
  - cfnetwork

must_haves:
  truths:
    - "Tester confirms CFNetworkCopySystemProxySettings returns __SCOPED__ keys with utun*/ipsec/ppp/tap/tun prefixes when a third-party VPN (Mullvad/WireGuard/Settings VPN) is active on iPhone 17 Pro Max"
    - "Tester confirms NWPath.usesInterfaceType(.cellular) returns true while a VPN tunnel is up over the cellular transport"
    - "Tester confirms iCloud Private Relay does not register a __SCOPED__ key with utun/ipsec/ppp/tap/tun prefix (or the workaround is documented)"
    - "Tester confirms NWPathMonitor's path-update callback fires on VPN up/down transitions on iOS 26"
    - "VERIFICATION-WAVE-0.md records a go/no-go decision before Wave 1 begins"
  artifacts:
    - path: ".planning/phases/08-vpn-context/08-VERIFICATION-WAVE-0.md"
      provides: "Device-test findings + go/no-go gate for the CFNetworkCopySystemProxySettings + NWPath broad-trigger detection mechanism"
      contains: "Decision: GO"
  key_links:
    - from: "08-VERIFICATION-WAVE-0.md"
      to: "08-RESEARCH.md detection mechanism (lines 156-174 reference impl)"
      via: "tester runs the 14-line CFNetworkCopySystemProxySettings probe and records key prefixes seen"
      pattern: "captureVPNActive|__SCOPED__|usesInterfaceType"
---

<objective>
Validate the four open device-test assumptions in 08-RESEARCH.md before any production code is written. The entire detection mechanism (Plan 02 schema, Plan 03 service code, Plan 04 UI) rests on these four findings being true on the actual iPhone 17 Pro Max running iOS 26.x.

Purpose: De-risk Wave 1+ by proving on-device that (a) `CFNetworkCopySystemProxySettings()` sees third-party VPN tunnels via `__SCOPED__` keys, (b) `path.usesInterfaceType(.cellular)` is true under VPN-over-cellular (this is the VPN-04 BROAD trigger correctness foundation), (c) iCloud Private Relay does not produce false positives, (d) NWPathMonitor fires on VPN transitions.

Output: `.planning/phases/08-vpn-context/08-VERIFICATION-WAVE-0.md` with raw findings and a GO / NO-GO decision. If NO-GO, this plan terminates the phase and triggers replanning with a different detection mechanism.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-vpn-context/08-CONTEXT.md
@.planning/phases/08-vpn-context/08-RESEARCH.md
@.planning/phases/08-vpn-context/08-PATTERNS.md

<interfaces>
<!-- Reference implementation extracted from 08-RESEARCH.md lines 156-174.
     Tester pastes this into a temp Swift file or Xcode playground on the
     iPhone 17 Pro Max device target to confirm behavior before any
     production change. -->

```swift
import SystemConfiguration

private func captureVPNActive() -> Bool {
    guard let cfDict = CFNetworkCopySystemProxySettings()?.takeRetainedValue() as? [String: Any],
          let scoped = cfDict["__SCOPED__"] as? [String: Any] else {
        return false
    }
    let prefixes = ["utun", "ipsec", "tap", "tun", "ppp"]
    for key in scoped.keys {
        let lowered = key.lowercased()
        if prefixes.contains(where: { lowered.hasPrefix($0) }) {
            return true
        }
    }
    return false
}
```

NWPath cellular check (existing API, no new code needed for the verification):
```swift
let monitor = NWPathMonitor()
monitor.pathUpdateHandler = { path in
    let usesCellular = path.usesInterfaceType(.cellular)
    print("path: \\(path), usesCellular: \\(usesCellular)")
}
monitor.start(queue: .main)
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: On-device verification of detection mechanism on iPhone 17 Pro Max</name>
  <read_first>
    - .planning/phases/08-vpn-context/08-RESEARCH.md (lines 127-229: Detection Mechanism + Capture Timing)
    - .planning/phases/08-vpn-context/08-RESEARCH.md (lines 230-308: VPN-04 Decision Tree, especially lines 257-263 for the broad trigger)
    - .planning/phases/08-vpn-context/08-CONTEXT.md (lines 30-42: D-06 narrow trigger, which the BROAD-trigger user override supersedes)
    - .planning/phases/08-vpn-context/08-PATTERNS.md (lines 386: Network.NWPath qualification rule — verifier should reproduce same import context)
  </read_first>
  <what-built>
    A self-contained verification harness (Xcode playground OR a temporary `VPNDetectionVerifier.swift` scratch file in the CellGuard target, gated behind `#if DEBUG`) that exercises the detection APIs on the iPhone 17 Pro Max and records the four findings.

    **No production code is touched in this plan.** Anything created in CellGuard for this verification must be deleted before Plan 02 begins (the verifier may live in a throwaway branch or a `.scratch/` directory excluded from git).
  </what-built>
  <how-to-verify>
    Tester executes the four checks below on the iPhone 17 Pro Max running iOS 26.x. For each check, record raw output (key names, boolean values, prefix matches) into `08-VERIFICATION-WAVE-0.md`.

    **Check 1 — CFNetworkCopySystemProxySettings sees third-party VPN:**
    1. Install Mullvad VPN (or WireGuard, or any commercially shipping iOS VPN client; alternatively configure an IKEv2 profile in Settings → General → VPN & Device Management).
    2. With VPN OFF: print all `__SCOPED__` keys returned by `CFNetworkCopySystemProxySettings()?.takeRetainedValue()["__SCOPED__"]`. Record the full list.
    3. With VPN ON: print all `__SCOPED__` keys again. Record the full list.
    4. Confirm: at least one key in the VPN-ON list has a prefix in `{utun, ipsec, ppp, tap, tun}` AND that key is NOT present in the VPN-OFF list (i.e. it appeared because of the user VPN, not Apple system services).
    5. If confirmed → write "PASS" with the observed prefix. If not confirmed → write "FAIL" with the observed key list and stop the phase.

    **Check 2 — path.usesInterfaceType(.cellular) under VPN-over-cellular:**
    1. Disable Wi-Fi on the device, leaving only cellular.
    2. Connect the third-party VPN (so the tunnel rides over cellular).
    3. Start an `NWPathMonitor`, capture the next path update, and record `path.usesInterfaceType(.cellular)`, `path.usesInterfaceType(.wifi)`, `path.usesInterfaceType(.other)`, and `detectPrimaryInterface(path)` equivalent (the first available interface's `type`).
    4. Confirm: `usesInterfaceType(.cellular) == true` AND the primary interface is `.other` (because VPN tunnels report `.other`). This is the EXACT condition the broad VPN-04 trigger depends on.
    5. If confirmed → "PASS". If not confirmed → "FAIL" with raw values; broad VPN-04 trigger is not implementable as specified, escalate.

    **Check 3 — iCloud Private Relay false-positive guard:**
    1. Disable any third-party VPN.
    2. Enable iCloud Private Relay (Settings → Apple ID → iCloud → Private Relay → On).
    3. With Private Relay ON and no other VPN, print all `__SCOPED__` keys.
    4. Confirm: NO key with prefix in `{utun, ipsec, ppp, tap, tun}` appears (or, if one does, document the literal key name and devise a workaround — likely an additional substring filter to exclude Apple's private relay path).
    5. If clean → "PASS". If false positive → "PASS WITH WORKAROUND" + record the exact key string and the proposed workaround filter (e.g. exclude keys containing `mask` or matching a specific prefix Apple uses for Relay). Plan 03 will incorporate the workaround. Only "FAIL" if no workaround is identifiable.

    **Check 4 — NWPathMonitor fires on VPN up/down transitions:**
    1. Start an `NWPathMonitor` and log every `pathUpdateHandler` invocation with a timestamp.
    2. Toggle the third-party VPN OFF → ON. Record number and timing of path-update callbacks (expect at least one within 5 seconds of the toggle).
    3. Toggle VPN ON → OFF. Record callbacks.
    4. Confirm: at least one callback per transition. If callbacks fire reliably → "PASS". If silent → "FAIL"; live dashboard binding (D-09 + RESEARCH live binding rec) cannot rely solely on path updates and a polling fallback must be added in Plan 03 → escalate.

    **Recording format:** Append findings to a NEW file at `.planning/phases/08-vpn-context/08-VERIFICATION-WAVE-0.md` with sections: Device, iOS version, VPN clients tested, Check 1 raw output, Check 2 raw output, Check 3 raw output (incl. workaround if needed), Check 4 raw output, Final Decision (GO / GO-WITH-WORKAROUND / NO-GO).

    **Do NOT proceed to Plan 02 unless Final Decision is GO or GO-WITH-WORKAROUND.**
  </how-to-verify>
  <action>
    Tester:
    1. Reads the four checks above and the reference impl in `<interfaces>`.
    2. Creates a scratch verifier (Xcode playground attached to device, OR a `#if DEBUG` button in CellGuard's existing UI that runs the four checks and prints to console) — implementation details are tester's discretion as long as the four findings are produced.
    3. Runs the four checks on the iPhone 17 Pro Max with iOS 26.x.
    4. Records all raw output in `.planning/phases/08-vpn-context/08-VERIFICATION-WAVE-0.md` using the template structure listed under "Recording format" above.
    5. Writes the Final Decision: `GO`, `GO-WITH-WORKAROUND`, or `NO-GO`.
    6. Deletes the scratch verifier code from CellGuard (or commits it to a throwaway branch). Production code stays untouched.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-vpn-context/08-VERIFICATION-WAVE-0.md && grep -E "Final Decision: (GO|GO-WITH-WORKAROUND)" .planning/phases/08-vpn-context/08-VERIFICATION-WAVE-0.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/08-vpn-context/08-VERIFICATION-WAVE-0.md` exists.
    - File contains a section "Check 1" with raw `__SCOPED__` key names and a verdict line "Check 1: PASS" or "Check 1: FAIL".
    - File contains "Check 2" with the four boolean values for `.cellular` / `.wifi` / `.other` / primary-interface and a verdict line.
    - File contains "Check 3" with iCloud Private Relay observations and either "PASS" or "PASS WITH WORKAROUND: <description>".
    - File contains "Check 4" with callback counts/timings and a verdict line.
    - File ends with `Final Decision: GO` or `Final Decision: GO-WITH-WORKAROUND` (NO-GO blocks the phase and triggers replanning).
    - No file in `CellGuard/` (production source tree) has been modified — `git status CellGuard/` shows no changes attributable to this plan.
    - If GO-WITH-WORKAROUND: the workaround filter (e.g. additional substring exclusion for Private Relay) is described literally enough that Plan 03's `captureVPNActive()` can implement it without further research.
  </acceptance_criteria>
  <resume-signal>Type "GO" to proceed to Plan 02, "GO-WITH-WORKAROUND: <one-line summary>" to proceed with the documented workaround, or "NO-GO: <reason>" to halt the phase for replanning.</resume-signal>
  <done>VERIFICATION-WAVE-0.md exists; final decision is GO or GO-WITH-WORKAROUND; tester confirms iPhone 17 Pro Max device-test findings unblock Wave 1.</done>
</task>

</tasks>

<verification>
- The detection mechanism that 08-RESEARCH.md depends on (`CFNetworkCopySystemProxySettings()` `__SCOPED__` scan) actually works on the target device with at least one third-party VPN client.
- The VPN-04 BROAD trigger condition `path.usesInterfaceType(.cellular) == true` while VPN tunnel is up over cellular is observable on the target device.
- iCloud Private Relay (the most likely false-positive source on a modern iPhone) is either non-interfering or has a documented workaround.
- The live-binding strategy in Plan 03 (`@Observable currentVPNState` updated from `handlePathUpdate`) is feasible because path-update callbacks fire reliably on VPN transitions.
</verification>

<success_criteria>
- [ ] `.planning/phases/08-vpn-context/08-VERIFICATION-WAVE-0.md` exists.
- [ ] All four checks have raw output and a PASS / FAIL / PASS-WITH-WORKAROUND verdict.
- [ ] Final Decision line is `GO` or `GO-WITH-WORKAROUND`.
- [ ] No production-code changes in `CellGuard/` from this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/08-vpn-context/08-01-SUMMARY.md` summarizing:
- The four findings (one line each).
- Final Decision and any workaround required.
- Hand-off to Plan 02 with explicit confirmation that the detection mechanism is unblocked.
</output>
