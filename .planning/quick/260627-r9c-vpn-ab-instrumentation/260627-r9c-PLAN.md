---
quick_id: 260627-r9c
title: VPN A/B instrumentation + probe hardening
status: ready
branch: feature/apple-support-script
---

# Quick Task 260627-r9c: VPN A/B instrumentation + probe hardening

Five changes to make CellGuard's data trustworthy for a VPN A/B experiment (reintroduce Tailscale, then Proton, measure whether either contributes to cellular silent failures) and to harden probe methodology. Build via xcodebuild MCP (iOS Simulator, iPhone 17 Pro) after changes. Atomic commits, one per change. Do NOT push. Branch: feature/apple-support-script (verify before first edit).

**Key files (from grep):** EventType is referenced/switched in `CellGuard/Models/ConnectivityEvent.swift`, `CellGuard/Helpers/DropClassification.swift`, `CellGuard/Models/SummaryReport.swift`, `CellGuard/Views/AnalyticsView.swift`, `CellGuard/Views/DropTimelineChart.swift`, `CellGuard/Views/EventListView.swift`, `CellGuard/Views/EventDetailView.swift`, `CellGuard/Services/EventStore.swift`. The new `.vpnStateChange` case must be handled in EVERY exhaustive switch (compiler will flag missing ones — build to find them all).

SwiftData note: additive OPTIONAL fields auto-migrate; no manual migration.

## Task 1 — Primary probe HEAD→GET with body validation
- files: CellGuard/Services/ConnectivityMonitor.swift
- action: In `runProbe()`, change primary request `httpMethod` from "HEAD" to "GET" (keep probeURL = captive.apple.com/hotspot-detect.html). Capture the response body (the `_` in `session.data(for:)`). Treat as `.probeSuccess` only if HTTP 200 AND body decoded as UTF-8 contains "Success" (Apple's exact captive payload `<TITLE>Success</TITLE>`). 200-but-wrong-body (transparent proxy/login page) → `.probeFailure` with reason like "unexpected body (captive portal?)". Preserve per-probe ephemeral `makeProbeSession()` and the two-host Cloudflare `confirmSilentFailure()` logic unchanged.
- verify: build succeeds; logic reads body and checks payload.
- done: primary probe is GET + body-validated; confirmation path untouched.

## Task 2 — Capture CTCellularData.restrictedState
- files: CellGuard/Services/ConnectivityMonitor.swift, CellGuard/Models/ConnectivityEvent.swift, CellGuard/Views/EventDetailView.swift
- action: Add retained `CTCellularData` instance; in `startMonitoring()` set its `cellularDataRestrictionDidUpdateNotifier` (populates restrictedState; unknown until first callback). Add `ConnectivityEvent.cellularDataRestricted: String?` ("restricted"/"notRestricted"/"unknown"). Read current restrictedState synchronously in `logEvent` like other metadata and pass through. Wire init + Codable (CodingKeys, encodeIfPresent/decodeIfPresent). Show a "Cellular Data Access" row in EventDetailView's Cellular section when present.
- verify: build succeeds; field persists + decodes.
- done: every new event records cellular-data restriction state; visible in detail view.

## Task 3 — Persist matched VPN interface key per event
- files: CellGuard/Services/ConnectivityMonitor.swift, CellGuard/Models/ConnectivityEvent.swift, CellGuard/Views/EventDetailView.swift
- action: Refactor `captureVPNDetectorBool()` so the matched interface key (e.g. "utun3") is available — extract a shared detection function returning (sortedKeys, matchedKey, matchedPrefix); have the bool path and a new `currentVPNInterface: String?` property (set alongside currentVPNState) use it. Add `ConnectivityEvent.vpnInterface: String?`. Snapshot it into `logEvent` the same way vpnState is (runProbe pre-await snapshot path AND transition path carry it). Wire init + Codable; place `vpnInterface` inside the SAME `if !omitLocation` export-gated block as `vpnState`. Show in EventDetailView VPN section when present.
- verify: build succeeds; export-gating matches vpnState.
- done: events carry the tunnel interface name; shared detection function (no duplication).

## Task 4 — Dedicated VPN state-transition event
- files: CellGuard/Models/ConnectivityEvent.swift, CellGuard/Services/ConnectivityMonitor.swift, and every file with an exhaustive EventType switch (DropClassification.swift, SummaryReport.swift, AnalyticsView.swift, DropTimelineChart.swift, EventListView.swift, EventDetailView.swift — build to confirm).
- action: Add `EventType.vpnStateChange = 6` (explicit rawValue). Wire displayName ("VPN State Change"), encodingString ("vpnStateChange"), fromEncodingString, and handle the case in ALL exhaustive switches (classification/charts/filters/summary). In `handlePathUpdate` after `currentVPNState = captureVPNState()`, detect crossing the tunnel-absent⟷present boundary (absent = {disconnected, invalid}; present = {connecting, connected, reasserting, disconnecting}); log a `.vpnStateChange` event (carrying new vpnState + vpnInterface) ONLY on that boundary crossing — track previous boundary side to avoid spam from connecting↔reasserting flicker. `scheduleDropNotification` MUST ignore `.vpnStateChange` (no "Cellular Drop Detected" notification).
- verify: build succeeds (all switches exhaustive); no notification on vpnStateChange.
- done: clean tunnel up/down timeline events; not treated as drops.

## Task 5 — In-app VPN detection self-check button
- files: CellGuard/Services/ConnectivityMonitor.swift (expose shared detection result), most fitting existing diagnostics/health view (e.g. HealthDetailSheet.swift — pick the best existing surface, do NOT create a new screen)
- action: Reuse the shared detection function from Task 3 (sortedKeys + matchedKey/prefix). Add a public method on ConnectivityMonitor returning a human-readable self-check result string ("matched=<key> (prefix <p>)" / "NO MATCH — keys=[...]" / "no proxy settings"). Add a "Run VPN Detection Self-Check" button in the chosen view that calls it and presents the result in an alert or sheet. Keep the existing once-per-launch os_log self-check too.
- verify: build succeeds; button shows live __SCOPED__ scan result on tap.
- done: user can verify VPN detection on-device with one tap, no Mac/Console/relaunch.

## Build & commit
- After all tasks: build via xcodebuild MCP (iOS Simulator, iPhone 17 Pro) → must BUILD SUCCEEDED.
- Atomic commit per task (code only). Do NOT push. Do NOT commit docs (orchestrator handles).
